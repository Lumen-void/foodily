# Foodily Contracts

Contract package contains:
- `openapi/foodily-v1.yaml` for public API route contracts.
- `schema/postgres.sql` for data model baseline.
- `types/domain.ts` for shared enum types.
