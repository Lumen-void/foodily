name: flutter_ui
description: Shared UI package for Foodily apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_core:
    path: ../flutter_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
